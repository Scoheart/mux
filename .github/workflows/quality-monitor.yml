name: Quality monitor (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    name: Automated tests are paused
    runs-on: ubuntu-latest
    steps:
      - name: Report fast delivery mode
        run: echo "Automated quality and test jobs are paused for fast direct delivery."
