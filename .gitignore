node_modules
dist
.agents
.claude/*
!.claude/skills
.superpowers
.worktrees/
.goose
.DS_Store

# local-only config (kept out of the repo)
/.mcp.json

# npm lockfiles are committed so CI and release builds can use deterministic
# `npm ci` installs and lockfile-keyed caches.
/package-lock.json

# Rust build artifacts (workspace)
/target
