[package]
name = "mux-core"
version = "1.1.2"
edition = "2021"
description = "Shared MCP config-management core for the MUX CLI and desktop app"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
chrono = "0.4"
dirs = "5"
ureq = "2"
